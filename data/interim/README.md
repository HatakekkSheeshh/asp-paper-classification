# Interim Data

Thư mục này chứa dữ liệu trung gian sau bước làm sạch nhưng trước khi tạo artifact cuối cùng.

Ví dụ:

- file train sau khi chuẩn hóa text/title
- bảng feature metadata đã parse từ `authors` và `doi`
- fold assignment để tái sử dụng giữa các notebook

Không commit file lớn trừ khi cả nhóm thật sự cần chia sẻ artifact đó.
